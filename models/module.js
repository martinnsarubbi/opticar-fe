class Module {
  constructor(id, title, color, iconImage, screen) {
    this.id = id;
    this.title = title;
    this.color = color;
    this.iconImage = iconImage;
    this.screen = screen;
  }
}
export default Module;